var output = [];
var count = 1;
function fizzBuzz() {
   if (count % 3 === 0){
   output.push("Ronald"); }
   else if (count % 5 === 0) {
       output.push("Baruelo");
   }
   else {
   output.push(count);
   }
   count++;
   console.log(output)
}
